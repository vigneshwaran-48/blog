import React from 'react';

interface Props {
    params: { name: string }
}

const page = ({ params: { name } }: Props) => {

    return (
        <div>
            
        </div>
    )
}

export default page;