import React from 'react';

interface Props {
    params: { id: number, name: string }
}

const page = ({ params: { id, name } }: Props) => {

    return (
        <div>

        </div>
    )
}

export default page;