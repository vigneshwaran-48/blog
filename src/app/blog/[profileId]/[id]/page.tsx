import React from 'react';

interface Props {
    params: { id: number, profileId: string }
}

const page = ({ params: { id, profileId } }: Props) => {

    return (
        <div>

        </div>
    )
}

export default page;